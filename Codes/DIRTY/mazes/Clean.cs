using System.Drawing;

namespace mazes
{
    public class Clean : WorldObject
    {
        public Clean(Point location) : base(location)
		{
        }

        public override void Act(IWorld world)
        {
            //for (int i = Location.Y - MouthLength; i <= Location.Y + MouthLength; ++i)
            //    for (int j = Location.X - MouthLength; j <= Location.X + MouthLength; ++j)
            //        if (i != Location.Y && j != Location.X)
            //        {
            //            world.Contains<
            //        }
                        //if (world.Contains<
                    //var destination = GetDirection(world);
                    //Destination = Location.Add(destination);
        }
    }
}
